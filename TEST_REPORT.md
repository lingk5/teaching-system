# teaching-system 回归测试报告

**测试日期:** 2026-03-31
**测试人员:** Codex (GPT-5)
**测试方式:** 本地自动化回归与源码级验证
**代码基线:** 当前工作区（含管理员界面恢复与角色权限收口）

---

## 📋 验证范围

- 后端权限回归：`backend/tests/test_role_permissions.py`
- UI/文档回归：`backend/tests/test_rbac_ui_regressions.py`
- 前端角色矩阵回归：`frontend/tests/auth-role.test.mjs`
- 语法与补丁校验：`python3 -m compileall`、`git diff --check`

---

## ✅ 测试结果总结

### 1. 角色权限与管理员界面

| 测试项 | 状态 | 详情 |
|--------|------|------|
| 管理员默认落点 | ✅ 通过 | `admin` 登录后默认进入 `users.html` |
| 管理员用户列表接口 | ✅ 通过 | `/api/auth/users` 对管理员返回 200 |
| 教师访问用户管理接口 | ✅ 通过 | `/api/auth/users` 对教师返回 403 |
| 助教课程导入越权 | ✅ 通过 | `/api/data/courses/import` 对助教返回 403 |
| 助教修改学生越权 | ✅ 通过 | `/api/courses/students/<id>` 对助教返回 403 |
| 助教处理预警越权 | ✅ 通过 | `/api/warnings/<id>/process` 对助教返回 403 |
| 管理员页面文件 | ✅ 通过 | `frontend/src/pages/users.html` 存在且已接真实接口 |

**验证要点:**
- ✅ `admin / teacher / assistant` 三角色页面与能力已重新分离
- ✅ 管理员用户管理页面与后端接口已恢复
- ✅ 前后端权限矩阵已统一，前端隐藏与后端 403 校验一致

---

### 2. 文档与页面结构回归

| 测试项 | 状态 | 详情 |
|--------|------|------|
| 登录页演示账号展示 | ✅ 通过 | `login.html` 中包含 `admin / teacher / assistant` |
| 登录页管理员跳转 | ✅ 通过 | `admin` 默认跳转 `users.html` |
| 预警页鉴权守卫 | ✅ 通过 | `warnings.html` 仍包含 `checkAuth` 与 `applyRoleUI` |
| 管理员控制台文案 | ✅ 通过 | `users.html` 包含管理员专用访问限制提示 |

**验证要点:**
- ✅ 论文和总文档可以据此同步为当前真实工程状态
- ✅ 管理员界面不是占位页面，而是已接真实用户管理 API 的可用页面

---

### 3. 自动化回归基线

| 测试项 | 状态 | 详情 |
|--------|------|------|
| 后端 `unittest` | ✅ 通过 | 共 11 条测试全部通过 |
| 前端 `node:test` | ✅ 通过 | 共 3 条测试全部通过 |
| Python 编译检查 | ✅ 通过 | `python3 -m compileall backend/app backend/tests` 通过 |
| Patch 校验 | ✅ 通过 | `git diff --check` 通过 |

**验证要点:**
- ✅ 当前仓库已经不再是“完全没有自动化测试”的状态
- ✅ 已具备最小可复用回归基线，适合论文中描述为“已建立基础回归验证机制”

---

## ⚠️ 当前仍未完全完成的部分

- `dashboard.html` 仍保留模拟趋势与兜底图表逻辑
- `analytics.html` 仍存在模拟数据兜底，尚未完全切到真实接口驱动
- `courses.py` 中课程/班级出勤率仍为占位值 `85`
- 教师资源所有权仍未完全收口为“仅访问自己课程”
- 成绩导出与预警引擎之间的权重口径仍未彻底统一

---

## 📊 整体评估

| 维度 | 状态 | 说明 |
|------|------|------|
| 功能完整性 | ✅ 良好 | 主流程与管理员控制台可演示 |
| 角色权限 | ✅ 良好 | 三角色界面和关键能力已分离 |
| 稳定性 | ✅ 良好 | 当前最小回归全部通过 |
| 数据真实性 | ⚠️ 待完善 | 仪表盘/学情分析仍有模拟数据 |
| 数据一致性 | ⚠️ 待完善 | 权重口径仍需进一步统一 |

---

## 🎯 建议

### 论文交付建议
1. 论文中可表述为“系统已完成主流程与基础 RBAC 控制，并建立最小自动化回归验证集”。
2. 不建议在论文中声称“所有图表均已完全真实化”或“系统已达到生产级完备状态”。
3. 对数据库现状应统一表述为“当前代码默认使用 MySQL，ORM 保留迁移能力”，不要再写成 SQLite 当前基线。

### 后续优化
1. 统一权重公式，消除预警、导出、前端展示之间的统计分裂。
2. 收口教师资源所有权，限制为仅访问本人课程/班级/学生。
3. 清理仪表盘和学情分析的模拟数据。
4. 扩展自动化回归到导入、导出、课程管理全链路。

---

## 📝 本次验证命令

```bash
cd /Users/a2914452089/Desktop/teaching-system
python3 -m unittest discover -s backend/tests -v
node --test frontend/tests/auth-role.test.mjs
python3 -m compileall backend/app backend/tests
git diff --check
```

---

**测试结论:** ✅ **当前仓库的角色权限、管理员界面与最小自动化回归基线验证通过，适合用于毕业设计演示与论文提交说明。**
